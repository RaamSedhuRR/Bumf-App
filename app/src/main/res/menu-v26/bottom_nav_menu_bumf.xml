<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:icon="@drawable/ic_baseline_home_24"
        android:iconTint="@color/white"
        android:tooltipText="@string/home_menu"
        android:id="@+id/home"
        android:title="@string/home_menu" />
    <item android:icon="@drawable/ic_outline_search_24"
        android:iconTint="@color/white"
        android:tooltipText="@string/search_menu"
        android:id="@+id/search"
        android:title="@string/search_menu"
        tools:targetApi="o" />
    <item android:icon="@drawable/ic_outline_manage_accounts_24"
        android:iconTint="@color/white"
        android:tooltipText="@string/profile_menu"
        android:id="@+id/profile"
        android:title="@string/profile_menu" />
</menu>